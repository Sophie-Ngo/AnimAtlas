    # print(source.selected.indices[0])
    # indexActive = source.selected.indices[0]
    # print(final_layout.children[0])

    # div = Div(args=dict(source=source, filter=bool_filter, time_slider=time_slider),
    #       text="<a href='#'>" + df['identified_name'][indexActive] + "</a>", width=50, height=100, stylesheets=[stylesheet])
    
    # final_layout.children[0] = div  # adjust the info on the right